{
  "name": "chubu-app",
  "version": "1.0.0",
  "description": "Aplicação desktop criada com Electron para carregar o Youtube Music",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --linux AppImage --win nsis --publish never",
    "build:linux": "electron-builder --linux AppImage --publish never",
    "build:win": "electron-builder --win nsis --publish never",
    "build:dir": "electron-builder --linux dir --publish never"
  },
  "build": {
    "appId": "com.victoraaquino.chubu",
    "productName": "Chubu",
    "icon": "src/resources/icon.png",
    "directories": {
      "output": "dist"
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "category": "AudioVideo"
    },
    "win": {
      "icon": "src/resources/icon.ico",
      "target": [
        "nsis"
      ]
    }
  },
  "devDependencies": {
    "electron": "latest",
    "electron-builder": "latest"
  }
}
